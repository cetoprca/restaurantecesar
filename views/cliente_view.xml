<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_res_partner_form_inherit_restaurante" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.restaurante</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <!-- Insertamos un nuevo grupo al final de la pestaña de ventas y compras -->
            <xpath expr="//page[@name='sales_purchases']" position="after">
                <page string="Restaurante">
                    <group>
                        <field name="es_cliente_restaurante"/>
                        <field name="cliente_id"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

    <!-- Action -->
    <record id="action_clientes" model="ir.actions.act_window">
        <field name="name">Clientes</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
